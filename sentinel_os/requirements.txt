fastapi==0.139.0
starlette>=1.0.1  # CVE-2026-48710 (BadHost) fix floor -- pinned explicitly, not just via fastapi's transitive bound
uvicorn==0.24.0
psycopg2-binary==2.9.12
anthropic==0.116.0
httpx<0.28  # anthropic 0.116.0 breaks on client construction ("unexpected keyword argument 'proxies'") with httpx>=0.28
pytest==7.4.0
numpy>=2.1,<2.9
pydantic>=2.9,<3  # fastapi 0.139.0 requires pydantic>=2.9.0
opentelemetry-api>=1.20
opentelemetry-sdk>=1.20
opentelemetry-exporter-otlp-proto-grpc>=1.20  # only needed if OTEL_EXPORTER_OTLP_ENDPOINT is set
redis==8.0.1
cryptography>=41.0  # twin_custody.py: ECIES envelopes, Ed25519/X25519, AES-GCM, HKDF
# The mandatory conservation boundary: governance_harness._write_decision routes
# every governed decision through conservation/boundary.py, which submits the
# episode->judgment transformation to conservation_kernel's verifier (via the
# vendored conservation/transport/ gateway) and fail-closes the ledger write on
# anything other than a clean acceptance. Not on PyPI; installed from the
# sibling repo. Pinned to a commit because conservation/transport/ + judgment.py
# mirror this kernel version's enums / Proposition rules / rejection codes --
# bump deliberately (re-run the conservation boundary tests on the new SHA).
conservation-kernel @ git+https://github.com/wking53214/Conservation_Kernel.git@25145aa0a10130c710dc71899c0da9930cf7d285
# The shared graph substrate (sage_k Node/Edge/Graph). Private; pinned to a
# commit like conservation-kernel above.
cns @ git+https://github.com/wking53214/cns.git@9f8f3fed8c33d7bd299c715e1416bed9bcbd0a1c
# The epistemic gate: epistemic/dit_gate.py judges whether the governor's
# free-text reasoning qualifies as evidence before it is sealed into the
# ledger's canonical hash. Private; pinned to a commit like the two above.
# NOTE: this SHA is DIT's feat/sync-evaluate head (the evaluate() API this
# module calls). Re-pin to the merge commit if that PR is squash-merged --
# a squash rewrites the SHA and this one becomes unreachable once the
# branch is deleted.
dit @ git+https://github.com/wking53214/DIT.git@26cee7b9632a0b2ceead6827eb1071271995acc5
